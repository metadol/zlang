export const MobileHeader = () => {
  return (
    <nav className=" h-[50px] bg-green-500 lg:hidden fixed top-0 w-full z-10">
      <h1>Mobile Header</h1>
    </nav>
  );
};

//later use this as the mobile footer with app like feature such as home, search, profile, etc.
